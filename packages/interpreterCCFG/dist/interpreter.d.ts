import * as ccfg from "ccfg";
import type * as types from "./types.js";
export declare class CCFGInterpreter {
    readonly ccfg: ccfg.CCFG;
    readonly sigma: Map<string, unknown>;
    readonly breakpoints: Set<number>;
    status: types.InterpreterStatus;
    stepCount: number;
    lastEvent: types.DebugEvent | undefined;
    lastError: unknown;
    private readonly debug;
    private readonly scheduler;
    private readonly clock;
    private readonly maxSteps;
    private readonly timeoutMs;
    private readonly threads;
    private readonly joinStates;
    private readonly eventChannels;
    private readonly eventTokenToChannel;
    private nextThreadId;
    private lastThreadIndex;
    constructor(ccfg: ccfg.CCFG, options?: types.CCFGInterpreterOptions);
    static fromCCFG(ccfg: ccfg.CCFG, options?: types.CCFGInterpreterOptions): CCFGInterpreter;
    reset(status?: types.InterpreterStatus): void;
    setBreakpoint(nodeUid: number): void;
    removeBreakpoint(nodeUid: number): void;
    clearBreakpoints(): void;
    setBreakpoints(nodeUids: number[]): types.Breakpoint[];
    getThreads(): types.ThreadSnapshot[];
    getCurrentThread(): types.ThreadSnapshot | undefined;
    getCurrentNode(): types.NodeSnapshot | undefined;
    getSnapshot(): types.InterpreterSnapshot;
    getStackTrace(threadId: number): types.StackFrame[];
    getScopes(threadId: number): types.ScopeSnapshot[];
    getVariables(variablesReference: number): types.VariableSnapshot[];
    execute(options?: types.RunOptions): Promise<types.StepResult>;
    run(options?: types.RunOptions): Promise<types.StepResult>;
    continueExecution(options?: types.RunOptions): Promise<types.StepResult>;
    resume(options?: types.RunOptions): Promise<types.StepResult>;
    step(options?: types.RunOptions): Promise<types.StepResult>;
    private advanceOne;
    next(options?: types.RunOptions): Promise<types.StepResult>;
    stepIn(options?: types.RunOptions): Promise<types.StepResult>;
    stepOver(options?: types.RunOptions): Promise<types.StepResult>;
    stepOut(options?: types.RunOptions): Promise<types.StepResult>;
    pause(): types.StepResult;
    stop(): types.StepResult;
    private visitNode;
    private visitFork;
    private visitAndJoin;
    private selectChoiceTarget;
    private executeNodeInstructions;
    private bindParameters;
    private executeInstruction;
    private evaluateEdgeGuard;
    private evaluateExpression;
    private resolveValue;
    private createEvaluationScope;
    private createEventChannel;
    private emitEvent;
    private waitEvent;
    private ackEvent;
    private getEventChannel;
    private findCorrespondingAndJoin;
    private createThread;
    private findThread;
    private encodeVariablesReference;
    private decodeVariablesReference;
    private endThread;
    private removeThread;
    private removeThreadAt;
    private result;
}
export declare function executeCCFG(ccfg: ccfg.CCFG, options?: types.CCFGInterpreterOptions & types.RunOptions): Promise<types.StepResult>;
